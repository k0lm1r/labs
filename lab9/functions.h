#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <stdio.h>
#include <conio.h>
#include <stdbool.h>
#include <stdlib.h>

void printFile(char*);
void fillFile(char*);
int stringLength(char*);
char getAnswer();
void reverseWord(char*, int, int);
char* readWord(char*, int);

#endif